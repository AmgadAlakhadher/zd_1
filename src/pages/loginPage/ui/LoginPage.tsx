import {Login} from '@/features/Login';
import { Header } from '@/widgets/Header';

const LoginPage = () => {
  return (
    <div>
      <Header />
      <Login />
    </div>
  );
};
export default LoginPage